/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "https://digital-hippo-production-037e.up.railway.app",
    ],
  },
};

module.exports = nextConfig;
